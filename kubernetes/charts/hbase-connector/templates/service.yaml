apiVersion: v1
kind: Service
metadata:
  name: {{ include "hbase-connector.fullname" . }}
  labels:
    {{- include "hbase-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: TCP
      name: {{ .name }}
    {{- end }}
  selector:
    {{- include "hbase-connector.selectorLabels" . | nindent 4 }}